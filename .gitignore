# ── Secrets ──
.env

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# ── Hugo build output ──
hugo-site/public/
hugo-site/resources/
hugo-site/.hugo_build.lock

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Local notes / reference files ──
1st rollback point 14042026.txt
Ciso AI Briefing audio execution steps.txt
article-image-demo.html

# ── Editor ──
.vscode/settings.json
.idea/
*.swp
*.swo

# ── Briefings ──
# Draft scripts (.md) ARE committed — they're the approved source text.
# MP3 files are NOT committed — they live in Cloudflare R2.
briefings/*.mp3

# NOTE: seen_urls.json is intentionally NOT ignored.
# It must be committed so deduplication persists between GitHub Actions runs.
